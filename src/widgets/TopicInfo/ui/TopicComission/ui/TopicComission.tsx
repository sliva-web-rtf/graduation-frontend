import { Stack } from '@mui/material';

type TopicComissionProps = {};

export const TopicComission = (props: TopicComissionProps) => {
    // const { description, result } = props;

    return (
        <Stack spacing={2} width="100%">
            Комиссия
        </Stack>
    );
};
